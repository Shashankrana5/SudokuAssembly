import Image from 'next/image'

export const SERVER_URI = "http://localhost:9090"

export default function Home() {
  return (
    <div>
      this is the page.tsx from app directory
    </div>
  )
}
